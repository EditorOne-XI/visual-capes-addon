{
	"format_version": "1.10.0",
	"particle_effect": {
		"description": {
			"identifier": "visualcapes:sky_burst",
			"basic_render_parameters": {
				"material": "particles_alpha",
				"texture": "textures/particle/particles"
			}
		},
		"components": {
			"minecraft:emitter_initialization": {
				"creation_expression": "v.max_height = 256;v.min_speed = 120;v.max_speed = 300;v.spread_speed = 10;v.size = 3;"
			},
			"minecraft:emitter_local_space": {
				"position": true,
				"rotation": true
			},
			"minecraft:emitter_rate_steady": {
				"spawn_rate": 2000,
				"max_particles": 1000
			},
			"minecraft:emitter_lifetime_once": {
				"active_time": 5
			},
			"minecraft:emitter_shape_point": {},
			"minecraft:particle_lifetime_expression": {
				"max_lifetime": "v.emitter_lifetime - v.emitter_age"
			},
			"minecraft:particle_motion_parametric": {
				"relative_position": ["v.particle_age < ((v.particle_random_3 * v.max_height) / (v.min_speed + v.particle_random_2 * (v.max_speed - v.min_speed))) ? 0 : Math.cos(v.particle_random_1 * 360) * (v.particle_age - ((v.particle_random_3 * v.max_height) / (v.min_speed + v.particle_random_2 * (v.max_speed - v.min_speed)))) * v.spread_speed", "v.particle_age < ((v.particle_random_3 * v.max_height) / (v.min_speed + v.particle_random_2 * (v.max_speed - v.min_speed))) ? ((v.particle_random_3 * v.max_height) - (v.particle_age * (v.min_speed + v.particle_random_2 * (v.max_speed - v.min_speed)))) : 0.5", "v.particle_age < ((v.particle_random_3 * v.max_height) / (v.min_speed + v.particle_random_2 * (v.max_speed - v.min_speed))) ? 0 : Math.sin(v.particle_random_1 * 360) * (v.particle_age - ((v.particle_random_3 * v.max_height) / (v.min_speed + v.particle_random_2 * (v.max_speed - v.min_speed)))) * v.spread_speed"]
			},
			"minecraft:particle_appearance_billboard": {
				"size": ["v.emitter_age > 2 ? v.size - (v.emitter_age - 2) : v.size", "v.emitter_age > 2 ? v.size - (v.emitter_age - 2) : v.size"],
				"facing_camera_mode": "rotate_xyz",
				"uv": {
					"texture_width": 128,
					"texture_height": 128,
					"flipbook": {
						"base_UV": [56, 0],
						"size_UV": [8, 8],
						"step_UV": [-8, 0],
						"frames_per_second": 6,
						"max_frame": 6,
						"stretch_to_lifetime": true
					}
				}
			},
			"minecraft:particle_appearance_tinting": {
				"color": [1, 1, 1, 1]
			}
		}
	}
}
